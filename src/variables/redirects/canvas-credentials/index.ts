import EN from "./EN.ts";
import ES_LA from "./ES_LA.ts";
import PT_BR from "./PT_BR.ts";

const CanvasCredentials: RedirectBrand = {
  brand: "Canvas Credentials",
  links: [...EN, ...ES_LA, ...PT_BR],
};

export default CanvasCredentials;
